# siksil/version + core — State 2026-08-26

## version repo
- Pages = `deploy-pages.yml` workflow (`build_type=workflow`, Actions ENABLED here only). Action refs = tags (`@v5`).
- `beta.json` = `2026.8.3.dev2` (default/generic-x86-64/qemux86-64), commit `4daee5b2`.
- Friend AramaKim pushes too — expect beta.json conflicts; resolve via fetch + reset + re-apply.

## core repo
- Tag = pyproject `version`; `build_it.sh amd64` syncs `inpui/const.py` + frontend pins.
- `dev2` latest digest `1ff4a74e5410...`. Contains (commits `d132dad89c9`, `da6487cdaeb`, `ed0f9939a25`, `4b120f7b458`):
  - pairing timeout 60s; ConfigEntries API `(entry, platforms)` fix
  - `effective_proxy_settings()` — Supervisor loopback proxy trust (fixes tunnel 400)
  - addon provisioning (File Editor, Terminal&SSH, Samba, AdGuard) + AdGuard split-DNS rewrites
  - `LocalSSLProvisioner`: wildcard cert → HTTPS :443 + :80 upgrade redirect
- Fork http config = storage-based (`_strip_meta` before schema when mutating); yaml ignored post-migration.
- Test gotchas: ServiceRegistry.async_call read-only → monkeypatch hass.services w/ SimpleNamespace; wipe `tests/testing_config/ssl/` between ssl tests.

## OTA flow
pyproject bump → build_it.sh amd64 → docker push → beta.json same string → push → hub Settings→Updates refresh.
