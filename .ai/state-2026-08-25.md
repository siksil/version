# siksil/version + core — State 2026-08-25

## version repo
- Pages = `deploy-pages.yml` workflow (`build_type=workflow`, Actions ENABLED on this repo only). Branch-based Pages builds die when repo Actions disabled — that was the stall.
- Workflow action refs = tags (`actions/configure-pages@v5`) — pinned SHAs must be real, fabricated ones fail resolution.
- `beta.json` current: `2026.8.3.dev1` for default/generic-x86-64/qemux86-64.
- Friend AramaKim (siksil2311@gmail.com) pushes here too — expect rebase conflicts on beta.json; resolve via `git fetch && git reset --hard origin/master` + re-apply.

## core repo
- `2026.8.3.dev1` built via `./build_it.sh amd64` + pushed (digest `1604b7a9c003...`). Tag = pyproject.toml `version`; script syncs `inpui/const.py` MAJOR/MINOR/PATCH + patches frontend pins from wheel filename in repo root.
- Wheel: `home_assistant_frontend-20260819.0-py3-none-any.whl` (friend-built; not on PyPI).
- Fixes in dev1: pairing timeout 10→60s (`api.py` pair_device); `async_forward_entry_setups(entry, PLATFORMS)` / `async_unload_platforms(entry, PLATFORMS)` — fork HA ConfigEntries API dropped `hass` arg; old 3-arg call = TypeError → setup abort → tunnel never spawns.
- cloudflared: spawned inside core container from `/bin/cloudflared` (Dockerfile COPY from cloudflare/cloudflared), stdout/stderr DEVNULL — check `ha core logs` for "Starting cloudflared tunnel..."
- Core container name in HAOS = `homeassistant` (not `core`).

## OTA flow
1. pyproject version bump → `./build_it.sh amd64` → `docker push ghcr.io/siksil/generic-x86-64-inpui-core:<ver>`
2. beta.json machine entries = exact same string → commit+push → Pages workflow deploys (~40s)
3. Hub beta channel → Settings→Updates refresh
